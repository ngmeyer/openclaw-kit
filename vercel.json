{
    "version": 2,
    "buildCommand": "echo 'No build step needed'",
    "installCommand": "echo 'No install step needed'",
    "public": true,
    "routes": [
        { "handle": "filesystem" },
        { "src": "/(.*)", "dest": "/website/$1" }
    ],
    "github": {
        "silent": true
    }
}